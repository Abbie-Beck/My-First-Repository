=begin

Iterating Over Hashes

hashes can have many elements in 1, so you will want to iterate 
over to interact with every element 
similar to iterating over arrays, some small diffs
  
  (exercise iterating_over_hashes.rb has the examples)
  
=end